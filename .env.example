# Optional: enable real LLM agent mode (OpenAI)
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini

# Agent mode: auto | llm | keyword
# auto = use LLM when OPENAI_API_KEY is set, else keyword matching
MCPGUARD_AGENT_MODE=auto
